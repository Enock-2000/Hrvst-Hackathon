# --- Secrets / credentials (DO NOT COMMIT) ---
# Real .env with passwords. The committed .env.example is exempt below.
.env
.env.*
!.env.example
*.env
!*.env.example

# Old hard-coded-creds compose snapshot
docker-compose.yml.backup
docker-compose.override.yml

# Eufy runtime data (auth tokens, cloud tokens, private keys, push creds, ...)
eufy-data/
persistent.json

# Large / generated test captures and debug artifacts
stream-test.json
*.log
.test.sh
.probe_ws.*
.first_frame.jpg
.live_frame.jpg

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Python / vision runtime
packhouse-runtime/.venv/
packhouse-runtime/.venv-locateanything/
**/__pycache__/
*.pyc
packhouse-runtime/runs/
packhouse-runtime/models/LocateAnything-3B/
*.pt
_test_*.jpg

# Local test captures (not for git)
camera_test_snapshot.jpg
